import {Theme} from "@material-ui/core";

export const materialSwitchStyles = (theme: Theme) => ({
    formLabel: {
        "-webkit-touch-callout:": "none",
        "-webkit-user-select": "none",
        "-khtml-user-select": "none",
        "-moz-user-select": "none",
        "-ms-user-select": "none",
        "user-select": "none"
    }
});